#include <iostream>
#include <cmath>

using namespace std;
float Sin(int x){
    return 1.0 - float(sin(x));
}
int main()
{
    int n,a,b;
    float h;
    cin>>n>>a>>b;
    

h=float(b-a)/float(n);
    for(float i=float(a);i<=b;i+=h){
        cout<<Sin(i)<<" ";
    }
}
